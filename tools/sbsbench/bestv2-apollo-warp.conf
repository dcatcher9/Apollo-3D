# Production and benchmark use the same validated Apollo quality profile. The harness itself
# pins one depth update per source frame and enables timing, so no operational overrides belong
# here. Add an individual sbs_3d_* key only for an explicit A/B treatment.
min_log_level = 2
sbs_3d_profile = apollo
