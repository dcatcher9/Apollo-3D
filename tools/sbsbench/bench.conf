# Pinned SBS configuration for the offline benchmark (run_eval.py passes this to
# `sunshine --sbs-bench`). Committed so every eval run — any machine, any session — uses the
# same knobs and results stay comparable to the committed baselines. If you change a value
# here, regenerate the baselines in the same commit (run_eval.py --update-baselines).
min_log_level = 2

# The production Apollo profile is the benchmark source of truth. The harness overrides only
# cadence (one inference per input frame) and telemetry, neither of which changes this contract.
sbs_3d_profile = vd3d
