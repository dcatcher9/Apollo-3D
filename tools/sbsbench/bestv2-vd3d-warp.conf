# Production and benchmark use the same validated VD3D quality profile. Explicit sbs_3d_* keys
# below this selector override the profile and are reserved for controlled A/B treatments.
min_log_level = 2
sbs_3d_profile = vd3d
